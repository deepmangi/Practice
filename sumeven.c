#include <stdio.h>
int main()
{
    int i,n, sum=0;
    printf("Enter Even No: ");
    scanf("%d"\n);
    for(i=2;i<=n;i+=2)
    {
        sum += 1;
    }
    Printf("Sum of all even No from 1 to %d is: %d" n,sum);
return 0;
}